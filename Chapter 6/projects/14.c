#include <stdio.h> 
#include <string.h>

#define MAX 255

int main(){

    char array[MAX];

    char ch;

    printf("Enter a line of input: ");

    int status;
    
    for (int i = 0; i <= MAX, ; i++){}

    for(int i = 0; i < strlen(array); i++){
        printf("%c", array[i]);
    }

}