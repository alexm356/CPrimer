#include <stdio.h>


int main(){

    int upper, lower, sumSquares;


    printf("Enter lower and upper limits: ");
    scanf("%d, %d", &upper, &lower);

    printf("The sum of squares between %d to %d is %d", upper, lower, sumSquares);

}