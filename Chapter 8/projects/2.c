#include <stdio.h>
#include <ctype.h>


int main(){

    char ch;

    int character;

    int decimalValue;


    while ((ch = getchar()) != EOF){

        if(ch == '\n'){

        }
        else if(ch == '\t'){

        }else if (ch == ' '){

        }else if()

        printf("Character: %c, Decimal %d", putchar(ch), decimalValue);
    }
}