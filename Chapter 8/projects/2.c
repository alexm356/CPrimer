#include <stdio.h>

int main(){

    char ch;

    int character;

    int decimalValue;


    while (getchar() != EOF){

        printf("Character: %c, Decimal %d", putchar(ch), decimalValue);
    }
}